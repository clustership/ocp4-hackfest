# AI installer playbook lab

Used to provision libvirt infrastructure to demonstrate Assisted Installer.

```
cp cluster-example.yml cluster.yaml
vi cluster.yml
cd ansible
./02-create-cluster.yml -v
```
